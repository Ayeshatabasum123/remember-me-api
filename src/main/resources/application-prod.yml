server:
  port: ${PORT:8080}
  forward-headers-strategy: framework

spring:
  application:
    name: remember-me-api

  datasource:
    url: jdbc:postgresql://postgres.railway.internal:5432/railway
    username: postgres
    password: gMJantQTQeHQNmIQWjPDeMrmLxWEPjqr
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  admin:
    create-test-admin: ${CREATE_TEST_ADMIN:false}